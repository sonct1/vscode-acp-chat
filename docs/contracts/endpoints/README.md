# Endpoint Contracts

Use this area for endpoint-level contracts grouped by resource or business capability.

Typical files:

- `<resource>.md`
- `<business-capability>.md`

Each endpoint document should describe method/path, purpose, auth, request, response, errors, compatibility notes, and examples.

Do not put broad service architecture, implementation task lists, or operational runbooks here.
